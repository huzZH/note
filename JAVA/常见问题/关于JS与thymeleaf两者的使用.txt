一般情况下，使用ajax异步获取数据是不能够再次调用thymeleaf模板引擎的，但是如果界面数据全都依靠异步获取的话
，可以通过将要显示的页面抽取出来作为一个html文件，这样异步调用的时候后台将值传到request域中，然后只要一个
页面即可，这个页面则按照thymeleaf模板获取数据，
但是需要注意的是，ajax的dataType的值必须为"html",不能够为json，且后台也不能够以json的数据格式发送
与此同时，contentType则是前台传给后台的值得类型，dataType是前台希望后台返回的值得类型