有包含日期字符串的json转换的时候会出错，需要设置一个DateFormat,此外还需要注意的是，SimpleDateFormat要尽量少创建，因为DateFormat和SimpleDateFormat
都不是线程安全的，在多线程环境下调用parse（）和format()方法应该使用同步代码来避免问题，与此同时，创建一个SimpleDateFormat对象需要消耗很大的代价，
每次处理一个时间信息的时候就创建一个，然后再丢弃这个对象，这样大量的对象就这样被创建出来，这样就占用了大量的内存和JVM控件